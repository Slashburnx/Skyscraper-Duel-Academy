# MongoDB connection string (MongoDB Atlas free tier works great — same as Bluegate setup)
MONGODB_URI=MONGODB_URI=mongodb+srv://slashdaniel42_db_user:qd5ursdLKHE6QSzs@cluster0.5zjptd5.mongodb.net/skyscraper?retryWrites=true&w=majority

# Secret used to sign admin login tokens — make this a long random string
JWT_SECRET=change-this-to-a-long-random-string

# The admin password for the site. This is only used ONE time, the first time the
# server ever starts with an empty database, to create the admin account.
# After that, the password lives (hashed) in the database and you change it from
# the site itself (Admin -> Change Password). Changing this value later does nothing.
INITIAL_ADMIN_PASSWORD=duelacademy2024

# Port the server runs on locally
PORT=3000
